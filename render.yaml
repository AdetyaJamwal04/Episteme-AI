services:
  - type: web
    name: episteme
    runtime: python
    buildCommand: pip install uv && uv sync --frozen --no-dev
    startCommand: uv run python -m uvicorn episteme.api.app:create_app --factory --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
